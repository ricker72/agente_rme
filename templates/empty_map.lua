-- Plantilla de mapa vacío para iniciar un proyecto RME.
-- Reemplaza el contenido con Lua generado por el asistente.

if not app.hasMap() then
  error("No se detectó un mapa cargado en RME.")
end

app.transaction(function(map)
  -- Aquí comienza tu script generado.
end)
